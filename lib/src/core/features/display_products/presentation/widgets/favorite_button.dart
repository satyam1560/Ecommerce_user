import 'package:flutter/material.dart';

class AddToWishlistIcon extends StatelessWidget {
  const AddToWishlistIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
      onPressed: () {},
      icon: const Icon(Icons.favorite_outline),
    );
  }
}
